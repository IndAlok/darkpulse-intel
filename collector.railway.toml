[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m darkpulse.cli collect-all --loop --interval 300"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
